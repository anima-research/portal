{
  "name": "@animalabs/portal-relay",
  "version": "0.4.0",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "bin": {
    "portal-relay": "./dist/src/index.js"
  },
  "description": "One Discord bot fronting many webhook personas: webhook + role pools, permission engine, WS gateway.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anima-research/portal.git",
    "directory": "portal-relay"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "start": "node --import tsx src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@animalabs/portal-protocol": "file:../portal-protocol",
    "@discordjs/opus": "^0.10.0",
    "@discordjs/voice": "^0.18.0",
    "discord.js": "^14.16.0",
    "prism-media": "^1.3.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
